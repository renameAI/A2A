# 비밀 — 절대 커밋 금지
.env

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.DS_Store
uploads/

# 머신 전용 프리뷰/에디터 설정
.claude/

# 런타임 산출물
cache/
audit/
pages/
# SQLite 영속 상태 (a2a.db·-wal·-shm)
data/
# Phase 4 CoT 데이터셋 산출물 (train/heldout/seal)
dataset/
.gstack/
# 구출된 학습 런 (adapter 가중치) — GB 단위, git 대상 아님
runs/

# 로컬 시크릿 백업 — 절대 커밋 금지
.env.bak*
# 팀 배포 스킬은 추적한다 (.claude의 나머지는 로컬 설정이라 무시 유지)
!.claude/
.claude/*
!.claude/skills/
saas.db
.vercel
